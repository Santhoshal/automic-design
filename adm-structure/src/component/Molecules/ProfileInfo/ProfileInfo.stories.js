import * as React from 'react';
import { ProfileInfo } from './ProfileInfo';

export default {
  title: 'Molecules/ProfileInfo',
  component: ProfileInfo,
};

const Template  = () => <ProfileInfo />;

export const Logo = Template.bind({});
Logo.args = {};
